{
  "name": "spectra-demo",
  "version": "1.0.0",
  "description": "Demo project showing Spectra coverage upload + score gating from GitHub Actions",
  "scripts": {
    "test": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "coverageReporters": ["lcov", "text"],
    "coverageDirectory": "coverage"
  }
}
